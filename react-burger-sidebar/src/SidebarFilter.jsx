import React from 'react';

const SidebarFilter = ({item}) => {

  return (
    <div
      style={{
        paddingLeft: '20px'
      }}
    >
      {item}
    </div>
  )
};

export default SidebarFilter;